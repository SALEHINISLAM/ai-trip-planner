import React from 'react';
import PropTypes from 'prop-types';

const Options = props => {
    return (
        <div>
            
        </div>
    );
};

Options.propTypes = {
    
};

export default Options;